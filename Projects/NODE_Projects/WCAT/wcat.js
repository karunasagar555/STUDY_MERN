const fs = require("fs");
let inputArr = process.argv;

let filesArr = [];

//Placing of FilesPath in Filesarray

for(let i=1; i<inputArr.length; i++)
{
    filesArr.push(inputArr[i])
}
console.log(filesArr);













